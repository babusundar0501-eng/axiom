# AXIOM Brain

From-scratch AI learning track: tokenizer, vocabulary, numerical encoding, embeddings, neural-network layers, loss, backpropagation, attention, transformer blocks, training and generation.

The first implementations are intentionally small and dependency-free so the mechanics are understandable.