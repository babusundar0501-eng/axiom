# AXIOM Backend

Secure boundary for hosted AI providers and publishing.

Rules:
- Provider secrets never reach browser clients.
- Users never paste provider API keys.
- Validate every App Blueprint.
- Treat generated code as untrusted until tested.
- Keep the browser MVP usable without this backend.

Planned services: planner, code generator, project storage, publishing, model gateway.
